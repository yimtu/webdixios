---
import { hero } from '../../data/hero';
---
<div class="hero-labels" aria-hidden="true">
  {hero.labels.map((item) => (
    <span class:list={['hero-label', item.className]}>{item.label}</span>
  ))}
</div>
